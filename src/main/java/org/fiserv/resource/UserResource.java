package org.fiserv.resource;import jakarta.enterprise.context.ApplicationScoped;import jakarta.inject.Inject;import jakarta.transaction.Transactional;import jakarta.ws.rs.*;import jakarta.ws.rs.core.MediaType;import jakarta.ws.rs.core.Response;import org.eclipse.microprofile.rest.client.inject.RegisterRestClient;import org.fiserv.DTO.UserDto;import org.fiserv.model.User;import org.fiserv.repository.UserRepository;import java.util.Objects;@Path("/user")@Produces(MediaType.APPLICATION_JSON)@Consumes(MediaType.APPLICATION_JSON)@Transactional@ApplicationScoped//@RegisterRestClientpublic class UserResource {    @Inject    UserRepository repository;    @POST    @Path("/signup")    public Response createUser(UserDto userDto) {        User user = new User();        user.setUsername(userDto.getUserName());        user.setPassword(userDto.getPassword());        System.out.println("userDetails:" + user.getUsername());        repository.persist(user);        return Response.ok("Account Created Successfully").build();    }    @POST    @Path("/login")    public Response logInUser(UserDto userDto) {        System.out.println("UserDto:" + userDto.getUserName());        User dbUser = repository.findByUsernameAndPassword(userDto.getUserName(), userDto.getPassword());        if (dbUser != null) {            return Response.ok("Login Successful").build();        }        return Response.ok("Invalid Username or Password").build();    }}